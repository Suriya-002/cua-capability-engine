# Evidence

Committed runs (discovery, replay-ok, replay-error, escalation) live here. Each run directory has a hash-chained
`events.jsonl`; verify with `cua evidence verify evidence/`. Screenshots are of a synthetic mock app only.
Capabilities are under `capabilities/`.
